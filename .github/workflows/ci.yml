name: ci

on:
  push: { branches: [main] }
  pull_request:

# Read-only by default. Without this the job token carries whatever the
# repository's default grants, and `actions/checkout` persists it into
# .git/config for the rest of the job — where every `cargo` invocation and
# every third-party action can read it.
permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      # Cheapest check first: a formatting failure should not wait on a build.
      - run: cargo fmt --all -- --check
      # The API explorer parses every .hurl file to build its endpoint list, so
      # listing exercises both the tool and the files — and needs no gateway, no
      # keys and no network. Without it this is the one part of the repo with no
      # automated coverage at all, and a broken parser would be found by whoever
      # next reached for the tool rather than here.
      - name: the API explorer can read every .hurl file
        run: ./deploy/test/api/api list > /dev/null
      - run: cargo clippy --workspace --all-targets -- -D warnings
      - run: cargo test --workspace

  # Two gates this repository documents as existing and which ran nowhere.
  #
  # A malformed `.tf` merged green until someone tried to apply it, and a new
  # advisory landed in silence — including one against a dependency the ignore
  # list in `.cargo/audit.toml` says must be "re-checked when the dependency it
  # names moves", which nothing was in a position to notice.
  #
  # Its own job because it shares nothing with the Rust build: no toolchain
  # cache to warm, no services, and it should report separately, since
  # "Terraform is malformed" and "a test failed" are different problems for
  # different people.
  deploy-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      # Formatting first, as in the Rust job: the cheapest failure should not
      # wait on provider downloads.
      - name: terraform fmt
        run: terraform fmt -check -recursive deploy/tofu

      # `-backend=false`, so no credentials and no state are needed — this
      # validates the configuration, not a deployment. Every stack and every
      # module, because a module is where a broken reference usually is and a
      # stack that does not use it would not surface one.
      - name: terraform validate
        run: |
          set -euo pipefail
          for dir in deploy/tofu/stacks/*/ deploy/tofu/modules/*/; do
            echo "== $dir"
            terraform -chdir="$dir" init -backend=false -input=false > /dev/null
            terraform -chdir="$dir" validate
          done

      # `validate` accepts a resource that can never render — a count gated on
      # a variable whose default is falsy and which no stack passes. That is
      # how the Cloudflare rate limit sat in the tree unreachable.
      - name: tofu reachability
        run: ./deploy/test/tofu-verify.sh

      # The mock upstream is what every request-path check trusts to say which
      # credential served a request. Nothing else can check it: a mock that read
      # the header wrongly would agree with itself and the scripts would pass.
      - name: mock upstream selftest
        run: MOCK_SELFTEST=1 python3 deploy/test/mock-upstream.py

      - uses: dtolnay/rust-toolchain@stable
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-audit
      # Reads `.cargo/audit.toml`, where every ignore has to say why the
      # advisory does not apply. An ignore that can never be re-checked is an
      # ignore that becomes permanent by accident.
      - name: cargo audit
        run: cargo audit

  # Everything above is pure and needs no services, which is the payoff for
  # keeping oag-router, oag-pool, and oag-proto free of I/O. Only the store and
  # server tiers need real infrastructure.
  integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: oag
          POSTGRES_PASSWORD: oag
          POSTGRES_DB: oag
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U oag" --health-interval 2s
          --health-timeout 3s --health-retries 20
      redis:
        image: redis:8-alpine
        ports: ["6379:6379"]
        options: >-
          --health-cmd "redis-cli ping" --health-interval 2s
          --health-timeout 3s --health-retries 20
    env:
      OAG_DATABASE__URL: postgres://oag:oag@127.0.0.1:5432/oag
      OAG_REDIS__URL: redis://127.0.0.1:6379
      OAG_SECURITY__SIGNING_SECRET: ci-only-signing-secret-0123456789abcdefghij
      # Must decode to EXACTLY 32 bytes. This one was 31 for the life of the
      # file and nothing noticed, because `migrate` did not parse the KEK
      # until validation moved into Config::validate.
      OAG_SECURITY__CREDENTIAL_KEK: Y2ktb25seS1rZWstMzItYnl0ZXMtMDEyMzQ1Njc4OTA=
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo run -p oag -- migrate
      # Migrations are idempotent and advisory-locked: running twice must be a
      # no-op, which is what makes it safe for every replica to migrate on boot.
      - run: cargo run -p oag -- migrate
      # The whole workspace, with the backends bound.
      #
      # Every gated test skips itself when these are unset, so without them it
      # passes here while testing nothing — and `check` above runs
      # `cargo test --workspace` with neither. That was true of `oag-store`
      # alone once; it is now true of fifteen tests across `oag` and
      # `oag-server` too: the sticky-pin lease, the admin panels' attempt
      # filter, the CLI headline, the revocation that could not evict, the
      # principal upsert's read-back. Each was written to fail when the fix it
      # guards is reverted, and each was proving that on a developer's machine
      # only.
      #
      # `--workspace` rather than a list of crates, so the next gated test
      # anybody adds is covered by where it lives rather than by remembering
      # to name its crate here.
      - run: cargo test --workspace
        env:
          OAG_TEST_REDIS_URL: redis://127.0.0.1:6379
          OAG_TEST_DATABASE_URL: postgres://oag:oag@127.0.0.1:5432/oag

  # The request path against mocks, not real providers. Separate from
  # `integration` so a store-test failure is not mixed with "aimock would not
  # start" and so we can install Node without paying for it on the pure job.
  verify-mocks:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: oag
          POSTGRES_PASSWORD: oag
          POSTGRES_DB: oag
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U oag" --health-interval 2s
          --health-timeout 3s --health-retries 20
      redis:
        image: redis:8-alpine
        ports: ["6379:6379"]
        options: >-
          --health-cmd "redis-cli ping" --health-interval 2s
          --health-timeout 3s --health-retries 20
    env:
      OAG_DATABASE__URL: postgres://oag:oag@127.0.0.1:5432/oag
      OAG_REDIS__URL: redis://127.0.0.1:6379
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: extractions/setup-just@v2
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: psql
        run: sudo apt-get update && sudo apt-get install -y postgresql-client
      - name: circuit breaker trips and the next request is refused
        run: ./deploy/test/breaker-verify.sh
      - name: OpenAI client over Anthropic mock (cross-dialect)
        run: ./deploy/test/translate-verify.sh
      - name: OpenAI and Gemini adapters against aimock
        run: ./deploy/test/dialects-verify.sh
      - name: Bedrock event-stream against VidaiMock
        run: ./deploy/test/bedrock-verify.sh
